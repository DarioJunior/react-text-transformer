function invertText(newInput) {
  let text = newInput;
  text = Array.from(text);
  return text.reverse();
};

function numericText() {

};

function csvText() {
  
};

function slugText() {
  
};

function vowelsText() {
  
};

function consonantsText() {
  
};

function varText() {
  
};

export {invertText, numericText, csvText, slugText, vowelsText, consonantsText, varText}